import LoginDisplay from "../../components/auth/LoginDisplay";

export default function Login () {
    // get data from API / Redux?

    // create onClick function/s for google / gitbub / local logins

    return <LoginDisplay />
}