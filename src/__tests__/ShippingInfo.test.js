import { render, screen } from '@testing-library/react';
import ShippingInfo from '../components/shipping/Shipping';

describe('ShippingInfo', () => {
    test('it has a shipping info component', () => {

    })
})
