import { render, screen } from '@testing-library/react';
import ProductListing from '../../features/products/products';

describe('Products', () => {
    test('it has a products listing feature', () => {

    })
})