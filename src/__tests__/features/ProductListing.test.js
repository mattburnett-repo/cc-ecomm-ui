import { render, screen } from '@testing-library/react';
import ProductListing from '../../features/products/productsSlice';

describe('Products', () => {
    test('it has a products listing feature', () => {

    })
})