import { render, screen } from '@testing-library/react';
import PaymentInfo from '../../features/payments/paymentsSlice';

describe('PaymentInfo', () => {
    test('it has a payment info feature', () => {

    })
})