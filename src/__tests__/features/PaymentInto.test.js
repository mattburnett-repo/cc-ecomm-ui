import { render, screen } from '@testing-library/react';
import PaymentInfo from '../../features/payments/payments';

describe('PaymentInfo', () => {
    test('it has a payment info feature', () => {

    })
})