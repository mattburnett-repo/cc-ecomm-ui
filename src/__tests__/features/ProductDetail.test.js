import { render, screen } from '@testing-library/react';
import ProductDetail from '../../features/product/product';

describe('ProductDetail', () => {
    test('it has a product detail feature', () => {

    })
})