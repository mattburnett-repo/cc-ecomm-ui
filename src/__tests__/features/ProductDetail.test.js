import { render, screen } from '@testing-library/react';
import ProductDetail from '../../features/product/productSlice';

describe('ProductDetail', () => {
    test('it has a product detail feature', () => {

    })
})