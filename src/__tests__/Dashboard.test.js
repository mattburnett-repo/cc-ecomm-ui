import { render, screen } from '@testing-library/react';
import Dashboard from '../components/dashboard/Dashboard';

describe('Dashboard', () => {
    test('it has a dashboard component', () => {

    })
})